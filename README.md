# Counter App (while loop)

Simple web counter that can increment/decrement a number and run a non-blocking `while` loop.

Usage:

1. Open [index.html](index.html) in your browser.
2. Use `+` and `−` to change the value manually.
3. Choose `Direction` and `Interval` (ms), then click `Start Loop` to run the loop; click `Stop` to end it.
